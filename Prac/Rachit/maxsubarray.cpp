#include<bit/sdc++.h>
using namespace std;
int maxSubArray(vector<int>& nums) {
        vector<long long> dp(n, 0);
        int n = nums.size();

        int high_so_far = nums[0];
        int high

        for(int i=0;i<n;i++){

        }	
}
int main(){

	vector<int> nums = {-2,1,-3,4,-1,2,1,-5,4};
	cout << "Ans: " << maxSubArray(nums) << endl;

	return 0;
}